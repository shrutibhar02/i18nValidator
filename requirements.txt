# No external dependencies required beyond the Python standard library
# This tool is designed to work with Python 3.6+ without additional packages

# Development dependencies
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
twine>=4.0.0
build>=0.8.0
